@model IEnumerable<Vidly2_0.Models.Movie>
@{
	ViewBag.Title = "Index";
}

@if (Model.Count() == 0)
{
	<p>No Movies yet.</p>
}
else
{
	<table class="table table-bordered table-hover">
		<thead>
			<tr>
				<th>Movies</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>@Html.ActionLink(item.Name, "Detail", "Movies", new { id = item.Id }, null)</td>
				</tr>
			}
		</tbody>
	</table>
}

